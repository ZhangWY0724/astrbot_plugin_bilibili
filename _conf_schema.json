{
    "sessdata": {
        "description": "sessdata",
        "type": "string",
        "hint": "sessdata 获取请查看 readme",
        "obvious_hint": true
    },
    "proxy": {
        "description": "proxy",
        "type": "string",
        "hint": "Bilibili API 请求代理地址。示例: http://127.0.0.1:7890 或 socks5://127.0.0.1:1080，留空则直连。",
        "default": ""
    },
    "interval_secs": {
        "description": "interval_secs",
        "type": "int",
        "hint": "单个UP主任务的检测周期（秒）。",
        "obvious_hint": true,
        "default": 300
    },
    "task_gap_secs": {
        "description": "task_gap_secs",
        "type": "int",
        "hint": "相邻两个UP主任务的最小间隔，秒数",
        "default": 20
    },
    "reconnect_silent": {
        "description": "reconnect_silent",
        "type": "bool",
        "hint": "重连时静默：启动时若长时间未成功推送订阅通知，则先静默一段时间以消化积压。",
        "default": false
    },
    "rai": {
        "description": "旧版图片渲染开关",
        "type": "bool",
        "hint": "仅在渲染方式为 auto 时生效，用于兼容旧配置。",
        "default": true
    },
    "render_mode": {
        "description": "动态渲染方式",
        "type": "string",
        "hint": "auto 兼容旧版 rai；plain 为纯文本；card 为插件卡片；native 为 Bilibili 原生页面截图。原生截图失败会依次降级为卡片和纯文本。",
        "options": ["auto", "plain", "card", "native"],
        "default": "auto"
    },
    "native_browser_install": {
        "description": "原生渲染浏览器安装策略",
        "type": "string",
        "hint": "auto 首次使用时自动安装 Chromium，并在 Linux 下自动补齐系统运行库；manual 仅提示手动安装；disable 禁用原生浏览器。",
        "options": ["auto", "manual", "disable"],
        "default": "auto"
    },
    "native_browser_timeout_secs": {
        "description": "原生页面截图超时",
        "type": "int",
        "hint": "打开 Bilibili 动态页面、等待容器和截图的超时时间（秒）。",
        "default": 30
    },
    "renderer_template": {
        "description": "renderer_template",
        "type": "string",
        "hint": "渲染模板",
        "options": ["template_1", "template_2", "simple"],
        "default": "template_2"
    },
    "node": {
        "description": "node",
        "type": "bool",
        "hint": "是否以转发消息形式推送动态消息",
        "default": false
    },
    "dynamic_limit": {
        "description": "dynamic_limit",
        "type": "int",
        "hint": "每个UP主的单次动态推送数量限制",
        "default": 5
    },
    "recent_dynamic_cache": {
        "description": "recent_dynamic_cache",
        "type": "int",
        "hint": "动态缓存大小（recent_ids长度）",
        "default": 4
    },
    "plain_push_template": {
        "description": "纯文本推送消息模板",
        "type": "text",
        "hint": "非图片推送模式下的动态消息模板，留空使用默认格式。可用变量: {name}(UP主名称) {uid}(UID) {action}(动态动作，如\"投稿了新视频\") {title}(标题) {text}(正文) {url}(链接)。格式化后的空行会被自动移除。图片和转发内容始终自动追加在模板文本之后。",
        "default": ""
    },
    "plain_push_forward_template": {
        "description": "纯文本推送转发内容模板",
        "type": "text",
        "hint": "非图片推送模式下的转发内容模板，留空使用默认格式。可用变量: {name} {uid} {title} {text} {url}，数据来源为被转发的原动态。",
        "default": ""
    }
}
